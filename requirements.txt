runpod
whisperx
boto3
numpy<2.0

# Explicitly install PyTorch for CUDA 12.1 and Python 3.11
# This might help resolve cuDNN library issues.
# The index-url will be handled by pip within the Docker build if specified directly in the RUN command, 
# or we can add the --index-url flag to the pip install command in the Dockerfile.
# For now, just listing the packages. The Dockerfile's pip install command will need the index URL.
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# torch and torchaudio are often included in the base image or pulled by whisperx
# If base image has a compatible torch, these might not be strictly needed here
# Forcing versions that worked with whisperx locally:
# torch==2.2.2
# torchaudio==2.2.2 